#include<string.h>

/**
 * _strchr - This program scans the string s if it contains the c
 * @s: The string that is to be scanned
 * @c: The character being searched for.
 *
 * Return: f
 */

char *_strchr(char *s, char c)
{
	char *f = (strchr(s, c));

	return (f);
}
