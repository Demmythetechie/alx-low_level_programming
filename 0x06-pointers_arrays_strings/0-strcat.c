#include "main.h"
#include<string.h>

/**
 * _strcat - This function concatenate two strings from the main function.
 * @dest: String to be concatenated to.
 * @src: string that is concatenated from.
 *
 * Return: dest
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
