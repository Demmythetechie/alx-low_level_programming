#include<string.h>

/**
 * _strcmp - This function returns an int when the vakue are compared.
 * @s1: First string 
 * @s2: Second string
 *
 * Return: Always cmp
 */

int _strcmp(char *s1, char *s2)
{
	int cmp = strcmp(s1, s2);

	return (cmp);
}
