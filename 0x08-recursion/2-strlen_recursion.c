#include<stdio.h>
#include<string.h>

/**
 * _strlen_rescursion - This program returns the length of string s.
 * @s: the length of the string being returned.
 *
 * Return: Always 0
 */

int _strlen _recursion(char *s)
{
	return (strlen(s));
}
