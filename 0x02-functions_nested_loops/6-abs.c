#include<stdlib.h>

/**
 * _abs - This program returns the absolute value of the the parameter x
 * @x: The parameter that is converted to the absolute value.
 *
 * Return: (x)
 */

int _abs(int x)
{
	return (abs(x));
}
