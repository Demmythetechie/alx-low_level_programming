#include<stdio.h>

/**
 * _puts - This program prints the string passed into the function.
 * @str: The parameter where the string is passed into.
 *
 * Return: void
 */

void _puts(char *str)
{
	puts(str);
}
