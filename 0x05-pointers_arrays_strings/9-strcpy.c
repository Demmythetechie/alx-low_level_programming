#include<string.h>

/**
 * _strcpy - This functiuons copies and return the value of src to dest.
 * @dest: The parmeter the string is pasted.
 * @src: The parameter the string is being copied from.
 *
 * Return: return (strcpy(dest, src));
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
