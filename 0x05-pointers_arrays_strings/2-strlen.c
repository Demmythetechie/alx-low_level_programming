#include<string.h>

/**
 * _strlen - This function returns the length of the string in the parameter.
 * @s: The string being checked
 *
 * Return: length
 */

int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
